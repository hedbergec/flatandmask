% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentation.R
\name{datamaskr}
\alias{datamaskr}
\alias{invoke_masking}
\title{Run a complete masking job}
\usage{
datamaskr(input_file, output_folder,
  key_file = file.path(output_folder, "masking_key.csv"),
  secret_key, mask_fields, progress_callback = NULL,
  missing_value_keyword_action = c("mask", "blank"),
  missing_value_keywords = c("NULL", "NA", "N/A", "NAN", "#N/A", "#NULL!",
    "NONE", "NIL", "MISSING", "UNKNOWN", "UNSPECIFIED", "UNDEFINED",
    "NOT APPLICABLE", "NOT AVAILABLE", "NO DATA", "NO VALUE"))

invoke_masking(input_file, output_folder,
  key_file = file.path(output_folder, "masking_key.csv"),
  secret_key, mask_fields, progress_callback = NULL,
  missing_value_keyword_action = c("mask", "blank"),
  missing_value_keywords = c("NULL", "NA", "N/A", "NAN", "#N/A", "#NULL!",
    "NONE", "NIL", "MISSING", "UNKNOWN", "UNSPECIFIED", "UNDEFINED",
    "NOT APPLICABLE", "NOT AVAILABLE", "NO DATA", "NO VALUE"))
}
\arguments{
\item{input_file}{Path to a \code{.csv} or \code{.json} file.}

\item{output_folder}{Directory where output files should be written.}

\item{key_file}{Path for the masking key CSV. Defaults to
\code{masking_key.csv} inside \code{output_folder}.}

\item{secret_key}{Secret key used as the HMAC-SHA256 key. Reusing the same
key with the same input value gives the same masked value.}

\item{mask_fields}{Character vector of CSV column names or JSON field paths
to mask. JSON paths may include or omit the \code{root.} prefix.}

\item{progress_callback}{Optional function called as
\code{progress_callback(stage, current, total, message)}, where \code{stage} is one
of \code{"load"}, \code{"mask"}, \code{"normalize"}, or \code{"export"}.}

\item{missing_value_keyword_action}{How to handle selected masked-field
values that exactly match \code{missing_value_keywords}. Use \code{"mask"} to create
deterministic masks, or \code{"blank"} to export blanks and omit those keyword
values from the masking key.}

\item{missing_value_keywords}{Character vector of database-style missing
value tokens to detect, compared case-insensitively after trimming.}
}
\value{
Invisibly returns \code{output_folder}. Files are written for their side
effects. CSV jobs write \code{data.csv}, \code{masking_key.csv}, and
\code{replicate_masking.R}. JSON jobs also write a masked JSON/NDJSON file and
normalized CSV tables.
}
\description{
\code{datamaskr()} is the primary scripted interface. It resets package state,
detects whether the input is CSV or one of the supported JSON shapes, masks
selected fields, writes output files, and mirrors progress through console
status messages or an optional callback.
}
\details{
\code{invoke_masking()} is retained as a compatibility alias for replication
scripts generated by earlier versions of JsonCSVMaskr and the original
PowerShell tool.
}
\examples{
input <- tempfile(fileext = ".csv")
writeLines(c("name,email", "Alice,alice@example.com"), input)
out <- tempfile()
datamaskr(input, out, secret_key = "demo", mask_fields = "email")
list.files(out)

}
\seealso{
\link{json_helpers}, \link{csv_helpers}, \link{shiny_app}
}
